# Copy to .env.local for deploy-wide defaults (optional).
# Without these, users can enter Supabase URL + key in the browser (Raum → Zahnrad).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Legacy alias (optional):
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
